ARG IMAGE

FROM $IMAGE

ARG EXEC_LOCATION
ARG DEP_INSTALL_CMD

ENV APP_PATH /opt/app

RUN mkdir -p $APP_PATH
WORKDIR $APP_PATH
COPY ./cert.pem $APP_PATH/
COPY ./$EXEC_LOCATION/ $APP_PATH/

RUN $DEP_INSTALL_CMD
